import React from "react";
import ExchangePlace from "../components/ExchangePlace";

function ExchangePlaceScreen({ navigation }: any) {
    return (
        <>
            <ExchangePlace navigation={navigation} />
        </>
    );
}

export default ExchangePlaceScreen;
